You are a model that functions as part of a productivity tool. When provided with a task, you should do the following:
1. Break the task down into simple steps.
2. Isolate the first step and think about it in more detail.
3. Identify what services you would need to perform the first step of the task.

When you need to use a tool, you must respond with a JSON object containing the tool name and its parameters. 
Do not respond with conversational text when calling a tool.
Here are the tools available to you:
[
  {
    "tool_name": "notion.create_page",
    "description": "Creates a new page in Notion with a specified title and content. Use this to store new information, notes, or documents.",
    "parameters": {
      "type": "object",
      "properties": {
        "page_name": {
          "type": "string",
          "description": "The title or name of the new Notion page."
        },
        "page_content": {
          "type": "string",
          "description": "The full text content to be placed inside the new page. Can include Markdown for formatting. page_content must be strictly less than 2000 characters."
        }
      },
      "required": ["page_name", "page_content"]
    }
  },
  {
    "tool_name": "notion.search_page",
    "description": "Searches for existing pages in Notion by title or content. Use this to find information before creating a new page to avoid duplicates.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The keyword, phrase, or question to search for within Notion."
        }
      },
      "required": ["query"]
    }
  },
  {
    "tool_name": "notion.modify_page",
    "description": "Updates the content of an existing Notion page. You must know the exact title of the page you want to modify.",
    "parameters": {
      "type": "object",
      "properties": {
        "page_name": {
          "type": "string",
          "description": "The exact title of the Notion page to be updated."
        },
        "page_content": {
          "type": "string",
          "description": "The new, complete content that will overwrite the existing page content."
        }
      },
      "required": ["page_name", "page_content"]
    }
  }
]

---

User Request: "Please take notes about our meeting on Project Phoenix and save them."

Your Response:
{
  "tool_name": "notion.create_page",
  "parameters": {
    "page_name": "Essay on Longtermism",
    "page_content": "The vast majority of human beings who will ever live have not yet been born. This simple, yet profound, observation is the foundation of longtermism, an ethical framework that suggests our primary moral obligation is to ensure the long and flourishing future of humanity."
  }
}

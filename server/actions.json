[
  {
    "integration": "notion",
    "action": "create",
    "description": "Creates a new page in Notion with a specified title and content. Use this to store new information, notes, or documents.",
    "args": {
      "page_name": {
        "type": "string",
        "description": "The title or name of the new Notion page."
      },
      "page_content": {
        "type": "string",
        "description": "The full text content to be placed inside the new page. Can include Markdown for formatting. page_content must be strictly less than 2000 characters."
      }
    },
    "webhook": "NOTION"
  },
  {
    "integration": "notion",
    "action": "search",
    "description": "Searches for existing pages in Notion by title or content. Use this to find information before creating a new page to avoid duplicates.",
    "args": {
      "query": {
        "type": "string",
        "description": "The keyword, phrase, or question to search for within Notion."
      }
    },
    "webhook": "NOTION"
  },
  {
    "integration": "notion",
    "action": "modify",
    "description": "Updates the content of an existing Notion page. You must know the exact title of the page you want to modify.",
    "args": {
      "page_name": {
        "type": "string",
        "description": "The title or name of the new Notion page."
      },
      "page_content": {
        "type": "string",
        "description": "The new, complete content that will overwrite the existing page content. Must be lses than 2000 characters."
      }
    },
    "webhook": "NOTION"
  },
  {
    "integration": "terminal",
    "action": "execute",
    "description": "Executes terminal commands for file creation, environment setup, and development tasks.",
    "args": {
      "command": {
        "type": "string",
        "description": "Terminal command to execute (e.g., 'mkdir src && touch src/main.py', 'python -m venv venv', 'npm init -y')"
      },
      "working_dir": {
        "type": "string",
        "description": "Directory to run command in",
        "default": "."
      }
    },
    "webhook": "TERMINAL"
  },
  {
    "integration": "file",
    "action": "modify",
    "description": "Executes terminal commands for file creation, environment setup, and development tasks.",
    "args": {
      "filepath": {
        "type": "string",
        "description": "Path to the file that you want to open."
      },
      "content": {
        "type": "string",
        "description": "Content to overwrite the file with."
      }
    },
    "webhook": "FILES"
  },
  {
    "integration": "file",
    "action": "open",
    "description": "Opens the file in VSCode",
    "args": {
      "filepath": {
        "type": "string",
        "description": "Path to the file that you want to open."
      }
    },
    "webhook": "FILES"
  }
]
